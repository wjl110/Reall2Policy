# Reall2Policy Windows 运行环境 — 使用说明

## 一、启动方式

### 方式 1（推荐，双击即可）
双击项目根目录下的 **`start.cmd`**，在新打开的命令窗口内执行 LeRobot 相关命令。该窗口已自动激活环境并设置全部环境变量。

### 方式 2（已有 CMD 窗口）
```
call activate.cmd
```

### 方式 3（PowerShell 用户）
```powershell
cmd /k activate.cmd
```
进入已激活的 CMD 子 shell。

> **提示**：启动脚本仅设置当前窗口及子进程的环境变量，不修改系统全局 PATH。环境依赖现有 Conda 入口 `D:\application\code\condabin\conda.bat`，请勿移动该 Conda 安装。

---

## 二、目录结构

| 目录/文件 | 用途 |
|---|---|
| `env\` | 独立 Python 3.12、PyTorch、FFmpeg 等全部依赖 |
| `lerobot\` | Seeed 验证版 LeRobot 源码（editable 安装），含 SO101 leader/follower 实现 |
| `cache\conda\`  `cache\pip\`  `cache\huggingface\`  `cache\torch\`  `cache\tmp\` | 各类缓存与临时文件，可安全清空以回收磁盘 |
| `data\` | LeRobot 默认数据/数据集/输出目录 |
| `calibration\` | 后续舵机与整臂校准数据存档目录 |
| `logs\` | 安装日志、命令帮助文本、验证报告 |
| `lab-log\` | 实验、踩坑、会话与节点台账（Agent 每次加载项目都会读写） |
| `activate.cmd` | 环境激活脚本（核心入口） |
| `start.cmd` | 双击启动脚本（调用 activate.cmd 后保留窗口） |
| `verify.cmd` + `verify.py` | 一键环境验证脚本 |

---

## 三、验证安装

在已激活的窗口（即上述启动方式打开的窗口）运行：

```
verify.cmd
```

该脚本会依次验证：
1. 所有关键 Python 包版本与 imports
2. SO101 leader/follower 类型是否注册到 CLI
3. NVIDIA GPU CUDA 是否可用（矩阵乘法自检）
4. FFmpeg 7.1.1 libsvtav1 AV1 视频编码
5. TorchCodec 视频解码回读
6. 核心 CLI 帮助命令（teleoperate / record / calibrate / train）
7. `pip check` 依赖一致性

本脚本不连接机械臂也不驱动任何硬件；会更新 `logs\verification.json` 与测试视频。

---

## 四、已安装版本（2026-09-08 验证通过）

| 组件 | 版本 | 说明 |
|---|---|---|
| Python | 3.12.14 (conda-forge, AMD64) | 官方 LeRobot 要求 3.12+ |
| LeRobot | 0.6.2 | Seeed 仓库 editable 安装，含 `[feetech]` 扩展 |
| PyTorch | 2.10.0+cu128 | CUDA 12.8 构建，适配 RTX 50 系列 |
| Torchvision | 0.25.0+cu128 | 与 PyTorch 同 CUDA 版本 |
| TorchCodec | 0.10.0 | 视频硬件/软件加速解码 |
| PyAV (av) | 15.1.0 | TorchCodec 不适用时的回退解码 |
| NumPy | 2.2.6 | |
| OpenCV (headless) | 4.13.0.92 | 无 GUI 依赖；`cv2.imshow` 不可用，使用 Rerun 可视化 |
| feetech-servo-sdk | 1.0.0 | STS3215 Feetech 总线舵机 SDK |
| FFmpeg | 7.1.1 | 含 libsvtav1、x264/x265 编码器 |
| NVIDIA 驱动 | 591.74 | CUDA 最高兼容版本 13.1 |
| GPU | RTX 5060 Ti 8GB | 实际显存占用取决于策略/图像尺寸/batch |

> 依赖快照：`logs\pip-freeze.txt`（pip 包版本）与 `logs\conda-explicit.txt`（Conda 精确包清单），可供重装或在其他机器复现。

---

## 五、Windows 与教程的差异（重要）

官方 Seeed 教程针对 **Ubuntu 22.04 x86_64** 和 **Jetson JetPack 6.0/6.1**，本环境为**原生 Windows**。请特别注意：

1. **串口名称**：Windows 为 `COM3`、`COM4` 等形式，**不要照抄**教程中 `/dev/ttyACM0`。第一次使用请运行：
   ```
   lerobot-find-port
   ```
   按提示拔插 USB 即可分别识别 Leader 与 Follower 端口。

2. **不要使用 Linux 命令**：`sudo`、`apt`、`chmod`、`/dev/*` 等在 Windows 不适用。Windows 串口一般不需要额外赋权，但需确认驱动板在设备管理器中识别为 USB 串行设备。

3. **符号链接权限**：训练时会在 `outputs\train\last` 等位置创建目录符号链接。若创建失败，建议用**管理员身份**打开 start.cmd，或在 Windows 设置中启用"开发者模式 → 允许开发人员创建符号链接"。

4. **OpenCV headless**：已按 LeRobot 上游使用 headless 构建，`cv2.imshow` 会报错。可视化使用 LeRobot 内置的 **Rerun**。

5. **FFmpeg PATH 已在 activate.cmd 中注入**：在激活的窗口中可直接使用 `ffmpeg`、`ffprobe` 命令，无需额外安装。

---

## 六、SO101 支持与常用命令

### 已启用功能
- ✅ `so101_follower` 从动臂机器人配置（`SOFollowerRobotConfig`）
- ✅ `so101_leader` 主臂遥操作配置（`SOLeaderTeleopConfig`）
- ✅ Feetech STS3215 舵机总线 SDK（`scservo_sdk` + `pyserial`）
- ✅ ACT 策略 `act` 模型训练与推理
- ✅ LeRobotDataset 数据读取/写入
- ✅ Hugging Face Hub 上传/下载

### 常用命令速查
```
lerobot-find-port --help            # 查找串口（区分 Leader/Follower）
lerobot-calibrate --help            # 舵机/整臂校准向导
lerobot-teleoperate --help          # 主从遥操作（数据采集前必试）
lerobot-record --help               # 录制演示数据集
lerobot-replay --help               # 回放已录 episode
lerobot-train --help                # 训练策略（ACT / Diffusion 等）
lerobot-eval  --help                # 在真实机械臂上评估策略
```

SO101 具体参数示例可参考：
- 源码内文档：`lerobot\docs\source\so101.mdx`
- 官方教程：<https://wiki.seeedstudio.com/cn/lerobot_so100m_new/>

---

## 七、尚未验证 / 后续步骤

本任务**只安装验证软件环境**，不驱动硬件运动。以下项目需待硬件连接后单独执行：

- [ ] **USB 串口驱动**：两张驱动板（Leader + Follower）在 Windows 设备管理器是否各出现一个 COM 口。
- [ ] **舵机 ID/波特率**：按教程使用 `lerobot-setup-motors` 为 12 只舵机分别设置 ID（L1-L6、F1-F6）。
- [ ] **整臂校准**：`lerobot-calibrate --robot.type=so101` 生成校准并存入 `calibration\`。
- [ ] **遥操作链路**：`lerobot-teleoperate --robot.type=so101` 验证主从实时跟随。
- [ ] **摄像头接入**：教程支持 OpenCV UVC、RealSense、Orbbec；当前仅安装了 OpenCV headless，若用 RealSense 需额外 `pip install pyrealsense2`。
- [ ] **策略训练**：显存 8GB 对大图像/大 batch 可能吃紧；实际能运行的超参需用 ACT 模板试跑后再定。
- [ ] **Hugging Face 登录**：若要上传数据集/模型，在激活窗口运行 `huggingface-cli login` 并粘贴 token。

---

## 八、参考资料

| 名称 | 链接 |
|---|---|
| Seeed SO101 × LeRobot 教程（中文） | <https://wiki.seeedstudio.com/cn/lerobot_so100m_new/> |
| LeRobot 官方安装文档（中英双语） | <https://hugging-face.cn/docs/lerobot/installation> |
| SO-ARM100/SO101 硬件项目（GitHub） | <https://github.com/TheRobotStudio/SO-ARM100> |
| Seeed 验证版 LeRobot 仓库 | <https://github.com/Seeed-Projects/lerobot> |
| RTX 50 系列 CUDA 注意事项 | 需 CUDA 12.8+（本环境 PyTorch cu128 已满足） |

---

> **维护提示**：不要直接升级 `lerobot`、`torch`、`torchcodec` 等核心包，上游更新频繁可能导致接口变动。如需升级，请先在另一目录克隆新环境测试。
