# 会话 2026-09-10

## 02:50 启动快照

- 终端：`activate.cmd` 窗口内 ACT **resume 训练中**，约 step 22K，loss≈0.121
- 最新数据：`so101_pick_place_20260910_011033`（20 ep / 9000 frames）
- 最新权重：`checkpoints/020000`
- 用户本轮意图：新建文件夹记录全部实验与踩坑，做成 Skill，每次加载项目都启动并持续记录

## 过程

- 02:50 创建 `lab-log/`、Skill `reall2policy-lab-log`、alwaysApply 规则、`AGENTS.md`
- 回填历史实验与 P001–P009
- GitHub `Reall2Policy` 仍待用户登录后创建（上一会话验证码已过期）

## 02:54 启动快照

- 终端：ACT resume 仍在跑，约 step 24K，loss≈0.111
- 最新数据：`so101_pick_place_20260910_011033`（20 ep / 9000 frames）
- 最新权重：`checkpoints/020000`
- 用户本轮意图：按 12 周求职级路线设置课程，只要可复制命令

## 过程

- 02:54 落地 `docs/COURSE.md` 与仓库骨架（configs / experiments / results / assets）
- README 改为作品集第一屏；E01 映射为当前 20ep ACT
- 训练未中断；明确禁止在训练读盘时 resume 同一数据集



## 03:03 启动快照

- 终端：ACT resume 仍在跑，约 step 30K，loss≈0.098
- 课表：W2=20/100，W3 E01 进行中；W4–W12 未开始
- 用户本轮意图：Skill 还要跟踪 `docs/COURSE.md` 的学习实践



## 过程

- 03:03 Skill 增加课表跟踪；新建 `lab-log/course.md`
- 同步 `docs/COURSE.md` 顶部 W3 为 ~30K/100K
- 规则 / AGENTS.md 要求每次加载对照课表汇报



## 03:13 杂数据目录

用户问 `data/local` 试录/空壳/中断目录要不要删。建议：训练读的 `..._011033` 绝对不删；空壳可删（减轻 P003）；中断与早期试录可删但不要拿去训练；`..._003232` 若还要 replay 就留。等 E01 训完再清。未执行删除。

## 03:19 用户确认已删

磁盘核对：`data/local` 只剩 `so101_pick_place_20260910_011033`（20 ep / 9000 frames）。训练仍在跑，约 step 39K，loss≈0.085。试录目录（含 replay 用的 `..._003232`）一并删掉。Git 工作区有对应删除，尚未 commit/push。

## 03:22 启动快照

- 终端：ACT resume，约 step 40K，loss≈0.082；**040000 已写入**
- 课表：W2=20/100，W3 E01 ~40%；整体完成度约 23%
- 用户本轮意图：Skill 增加学习进程、项目收益、学习曲线、数据回收图

## 03:28 GitHub push 频率

用户问要不要固定 push 周期。结论：不要定时自动推；按里程碑手动推（清数据、训完一段、改课表/代码）。权重 `outputs/` 本来就不进 Git。杂数据删除仍未 push。

## 过程

- 03:22 增加 `lab-log/metrics.json`、`lab-log/charts.md`、Skill `charts-spec.md`
- 对话看板 Canvas：`learning-dashboard.canvas.tsx`



## 22:43 真机成功 + 下一步

- E01 100K 已完成（05:03）；权重 `checkpoints/100000`
- 用户确认：工作姿态下 rollout **完全 OK**
- 折叠态抖动记为 P010
- 写入 `experiments/2026-09-10_rollout-e01.md`；课表下一步 = W2 +30→50



## 过程

- 22:43 更新 CURRENT / INDEX / course / COURSE / metrics / charts / pitfalls P010 / experiments INDEX



## 22:47 camera 截图命令

用户问截图命令。本机：`lerobot-find-cameras opencv`（图默认在 `outputs/captured_images`）。

## 22:50 实时画面

用户问摄像头实时画面。本机 OpenCV headless，不能 `cv2.imshow`；用遥操 + `--display_data=true` + cameras，Rerun 出画面。

## 22:54 默认看着画面录

用户要求之后命令一律带 `--display_data=true`。已改 `docs/COURSE.md` 全部 record/teleop/rollout、Skill 硬规则、`run-eval.cmd`。

## 22:58 补采被 Esc 停

日志：`Escape key pressed. Stopping data recording...`（P011）。不是崩溃。本轮 5 集已写入，总数 25/100。Rerun 1GiB 内存告警只丢历史画面，不是停录原因。

## 23:17 录制完成

`info.json`：**50 episodes / 27000 frames**。本轮 25 集，29.98 Hz，正常 Stop recording。下一步课表：训 E02。

## 23:24 解释 E02 训练日志

终端 `11.txt` 显示 E02 已启动：`outputs\train\act_so101_e02`，50 ep / 27K frames，100K step，batch 8。step 200 loss 7.076 → step 400 loss 3.190，~9.8 step/s，ETA ~2h50m。用户只要求解释日志。已写入 `experiments/2026-09-10_act-train-e02.md`。

## 23:26 E02 崩溃（P012）

不是 Esc。step 614 DataLoader worker 3 找不到 `file-002.mp4`。`file-001/002` 创建时间 23:24:56（与崩溃同一秒）；`file-000` 仍是凌晨 20 集。事后三份 mp4 可解码，合计 27000 帧。无 ckpt，从头重训。

## 23:35 50 集怎么拆、E02 已重训

用户问 9000+3000+15000 是 45 还是 50。元数据：**50 ep**。file-000 = ep0–19（20×15s=9000）；file-001 = ep20–24（5×20s=3000）；file-002 = ep25–49（25×20s=15000）。27000÷600=45 是误把第一批也当成 20s。终端 E02 重训已过 614，~4K loss≈0.69。

## 23:38 解释 resume

用户问「不要对同一数据集 resume 录制」。`lerobot-record --resume=true` = 往已有文件夹里**接着加集**（晚上补采 50 就是这样）。训练正在读 `..._011033` 时不要再往里写。这和训练命令里的 `--resume=true`（从 checkpoint 接着训）不是一回事。E02 现 ~6K，loss≈0.45。

## 23:46 训练和录制能否同时

用户确认：不允许训练和录制同时进行。是。E02 ~10K，loss≈0.264。

## 23:47 CPU/GPU 快照

i7-14650HX 负载约 30–40%；RTX 5060 Ti 93%、5347/8151 MiB、68°C、135W；`env\python.exe` 在训。E02 ~11K loss≈0.255。另有 rerun.exe 仍在。

## 23:56 超频/加速

用户问能否超频加速。结论：这场 E02 不要超（~16K，20K 才有 ckpt）。笔记本 HX 不建议 CPU 超频。加速留给下一场：`--policy.use_amp=true`，必要时再加大 batch。Windows 电源「最佳性能」可开，不必停训。

## 23:57 README Demo GIF

用户问当时是否要把录制视频转 GIF 放到首页 README。核对 02:54 课表原文：第一屏标题后直接 **Demo GIF**；仓库骨架里另有 `assets/demo.mp4`。落地时 README 只写了「Week 4 后放 `assets/demo.mp4`」，没有 GIF。`assets/` 仍空。正确来源是 W4 拔 Leader 后的真机 Demo，不是数据集 mp4。E02 仍在训（~16K），未转 GIF。

## 00:45 Demo GIF 入库

上一轮未 commit。原 `Captures/ShowCase.gif` 约 248MB（720x1280、60s），超 GitHub 100MB。已压成 `assets/demo.gif`（360x640、8fps、约 47MB）写入 README Demo。未把原 248MB 文件加入 Git。

## 00:49 第二段 Demo 并排

`assets/demo_0.gif` 原约 213MB（1080x1920、23s）。已就地压成 360x640 / 8fps / 约 18MB。README Demo 用表格并排 `demo.gif` + `demo_0.gif`。

## 01:13 README 同步机制落地

用户同意设计。新增 `scripts/sync-readme.py`、`sync-readme.cmd`；`metrics.json` 增 `readme` 段；README 加 `<!-- readme:* -->` 标记块。Skill/AGENTS/templates 写入里程碑触发规则。同步 lab-log/COURSE/experiments 至 E02 ~58K。

## 00:51 压缩前原片

用户要求删除压缩前 GIF。磁盘已无原片：`Captures/ShowCase.gif` 不存在；`demo_0.gif` 压缩时已覆盖。现仅剩 `assets/demo.gif`（47MB）与 `assets/demo_0.gif`（18MB）。未再删。

## 01:01 README GIF 说明

两张图下方分别加描述：左为斜视遥操抓放，右为同任务侧视。E02 已到 ~50K。

---

