# 会话 2026-09-10

## 02:50 启动快照

- 终端：`activate.cmd` 窗口内 ACT **resume 训练中**，约 step 22K，loss≈0.121
- 最新数据：`so101_pick_place_20260910_011033`（20 ep / 9000 frames）
- 最新权重：`checkpoints/020000`
- 用户本轮意图：新建文件夹记录全部实验与踩坑，做成 Skill，每次加载项目都启动并持续记录

## 过程

- 02:50 创建 `lab-log/`、Skill `reall2policy-lab-log`、alwaysApply 规则、`AGENTS.md`
- 回填历史实验与 P001–P009
- GitHub `Reall2Policy` 仍待用户登录后创建（上一会话验证码已过期）

## 02:54 启动快照

- 终端：ACT resume 仍在跑，约 step 24K，loss≈0.111
- 最新数据：`so101_pick_place_20260910_011033`（20 ep / 9000 frames）
- 最新权重：`checkpoints/020000`
- 用户本轮意图：按 12 周求职级路线设置课程，只要可复制命令

## 过程

- 02:54 落地 `docs/COURSE.md` 与仓库骨架（configs / experiments / results / assets）
- README 改为作品集第一屏；E01 映射为当前 20ep ACT
- 训练未中断；明确禁止在训练读盘时 resume 同一数据集

## 03:03 启动快照

- 终端：ACT resume 仍在跑，约 step 30K，loss≈0.098
- 课表：W2=20/100，W3 E01 进行中；W4–W12 未开始
- 用户本轮意图：Skill 还要跟踪 `docs/COURSE.md` 的学习实践

## 过程

- 03:03 Skill 增加课表跟踪；新建 `lab-log/course.md`
- 同步 `docs/COURSE.md` 顶部 W3 为 ~30K/100K
- 规则 / AGENTS.md 要求每次加载对照课表汇报

## 03:13 杂数据目录

用户问 `data/local` 试录/空壳/中断目录要不要删。建议：训练读的 `..._011033` 绝对不删；空壳可删（减轻 P003）；中断与早期试录可删但不要拿去训练；`..._003232` 若还要 replay 就留。等 E01 训完再清。未执行删除。

## 03:19 用户确认已删

磁盘核对：`data/local` 只剩 `so101_pick_place_20260910_011033`（20 ep / 9000 frames）。训练仍在跑，约 step 39K，loss≈0.085。试录目录（含 replay 用的 `..._003232`）一并删掉。Git 工作区有对应删除，尚未 commit/push。

## 03:22 启动快照

- 终端：ACT resume，约 step 40K，loss≈0.082；**040000 已写入**
- 课表：W2=20/100，W3 E01 ~40%；整体完成度约 23%
- 用户本轮意图：Skill 增加学习进程、项目收益、学习曲线、数据回收图

## 过程

- 03:22 增加 `lab-log/metrics.json`、`lab-log/charts.md`、Skill `charts-spec.md`
- 对话看板 Canvas：`learning-dashboard.canvas.tsx`
## 22:43 真机成功 + 下一步

- E01 100K 已完成（05:03）；权重 `checkpoints/100000`
- 用户确认：工作姿态下 rollout **完全 OK**
- 折叠态抖动记为 P010
- 写入 `experiments/2026-09-10_rollout-e01.md`；课表下一步 = W2 +30→50

## 过程

- 22:43 更新 CURRENT / INDEX / course / COURSE / metrics / charts / pitfalls P010 / experiments INDEX

## 22:47 camera 截图命令

用户问截图命令。本机：`lerobot-find-cameras opencv`（图默认在 `outputs/captured_images`）。

## 22:50 实时画面

用户问摄像头实时画面。本机 OpenCV headless，不能 `cv2.imshow`；用遥操 + `--display_data=true` + cameras，Rerun 出画面。

## 22:54 默认看着画面录

用户要求之后命令一律带 `--display_data=true`。已改 `docs/COURSE.md` 全部 record/teleop/rollout、Skill 硬规则、`run-eval.cmd`。

## 22:58 补采被 Esc 停

日志：`Escape key pressed. Stopping data recording...`（P011）。不是崩溃。本轮 5 集已写入，总数 25/100。Rerun 1GiB 内存告警只丢历史画面，不是停录原因。

## 23:17 录制完成

`info.json`：**50 episodes / 27000 frames**。本轮 25 集，29.98 Hz，正常 Stop recording。下一步课表：训 E02。







## 03:28 GitHub push 频率

用户问要不要固定 push 周期。结论：不要定时自动推；按里程碑手动推（清数据、训完一段、改课表/代码）。权重 `outputs/` 本来就不进 Git。杂数据删除仍未 push。
