# 会话 2026-09-13

## 20:21 大屏全屏被顶掉

点全屏约 2 秒就退出。根因是整页 `meta refresh` + watcher 重写 HTML（P022）。已改成静态页拉 JSON，本机 http://127.0.0.1:8765。只重启 watcher，训练未停。

## 20:18 大屏倒计时

用户要实时倒计时。停掉两个旧 ACT-v3 watcher。`watch-train.py` 改读 DP 日志 tqdm。大屏 `lab-log/dp-countdown.html`。当时约 1924 步、1.24 s/step，剩约 34 h，预计周二 06:00 前后。训练未停。

## 20:10 问要不要 use_amp / 加速

用户问是否开 `--policy.use_amp=true` 和加速。答：**不要改正在跑的。** 本场已有 `accelerator.mixed_precision=bf16`，训练走 `accelerator.autocast()`。`use_amp` 是旧开关，主要给 eval/rollout，加了也要停训重开，解不了 40W。不要超频、不要加大 batch。

## 20:05 问 48h 正不正常

用户问久得是否正常。当时 ~1526 步、**2.4 s/step**、ETA ~65 h。INFO：data_s 0.028 正常，**updt_s 1.60**（早先 0.17–0.36）。GPU 100% / **40W** / 54°C。答：DP 比 ACT 久是正常（263M、100K）；掉到 60+ 小时不正常，像功耗被掐，不是卡死。不要 Ctrl-C。

## 20:00 问训完几点

DP ~1324 步。约 1215 起掉到 **1.73 s/step**，tqdm 剩约 48 h。按这个：**周二 9/15 约 19:30**。若回到 2.5 step/s 则周一早上；回到 5 step/s 则今晚凌晨。GPU 94% / 52W / 59°C，不像烫降频。20K 按慢速大约周一 05:00。

## 19:56 问 W8 / DP 含义

用户问：W8 是不是 DP、DP 是什么、和前面差什么、命令行为何久、是否官方集。答：是 Diffusion Policy；同一 100 ep 换算法；没用 Hub 集。慢是 263M + 100K + 首步预热（先显示 363h）。当时 step ~1K loss 0.047，速度从 5 掉到约 2.4 step/s。

## 19:51 本周总结

用户要本周完成事项。日历周 09-08～09-13：W0–W7 齐，W8 刚开。磁盘 100 ep / 65100 帧；日常仍 E03 37/90。DP 第一次缺 `diffusers`（P021），19:50 重开已过 Creating policy，步进约 4.8 step/s、263M。画布 `week-recap-2026-09-13.canvas.tsx`。数字未变，不重画四张图。

## 19:48 开 W8 Diffusion

用户确认今天开 W8。第一次缺 diffusers（P021）。装上后 19:50 开训，100 ep，step 200 loss 0.564，约 4.9 step/s。不要录、不要开 VLA。ACT 对照用 E03。

## 19:44 ACT-v3 Position 13/90

物体在工作区、臂换角度，6×15。分轮 0/6/1/0/2/4。E03 Position 0/90，v2 0/15。飞轮在找物上兑现了；IID 仍 8/90。日常演示仍 E03。W7 表齐，不要开 DP。

## 19:34 问做这些有没有意义

用户在 v3 IID 8/90 后问价值。结论：课表闭环有意义；v2/v3 两次从零 100K 没有把日常策略变强。日常仍 E03。不要再用「再训一轮」证明有意义。Position 15 只补对照表，不改变结论。

## 19:27 ACT-v3 IID 8/90

6×15，夹下 + 工作态。分轮 1/2/2/1/0/2。失败主因夹前头再往前推（P020）。弱于 E03 37/90，好于 v2 0/15。日常仍 E03。下一步 Position 15。

## 19:12 用户确认训完

磁盘仍是 `act_so101_v3/checkpoints/100000`（step=100000）。数字未变。下一步按课表做 v3 IID 15。提醒：必须先 `start.cmd`；不要用 v2 权重。

## 15:41 ACT-v3 100K

磁盘 **100000 @15:41:08**。训练进程已退出。100K loss 无日志。watch-train 与 90s 循环已停。下一步：IID 15 → Position 15。日常仍用 E03。

## 15:13 ACT-v3 80K

磁盘 **080000 @15:12:02**。进程仍在。80K loss 无日志。下一档 100K。

## 14:43 ACT-v3 60K

磁盘 **060000 @14:42:56**。进程仍在。60K loss 无日志。下一档 80K。

## 14:21 官方提效

用户觉得采集/训练太慢。结论：官方 ACT 本就是从零数小时；慢在五次满 100K 和条太长，不是缺 Hub ACT 权重。能复用的是 W9 `smolvla_base` 20K、下一轮从 ckpt 接着训、打开 image_transforms、放下就停。不要混官方集、不要 π₀/Jobs。不改 v3。画布 `hf-efficiency.canvas.tsx`。

## 14:21 窗口 aborted，训练没停

Cursor 把 train / watch-train / 90s loop 标成 aborted。磁盘 **040000 @14:13:38**，进程 42372 仍在，GPU 仍占用。估计 ~45K。已重挂刷新。不要 resume。

## 14:13 写入 W9 课表命令

用户要求现在改 `docs/COURSE.md`。已换成 `--policy.path=lerobot/smolvla_base`，数据 `local/so101_lang`（先录语言指令，再训），batch 4、20K、不上 Hub。补了第一批 record 命令。`configs/smolvla.yaml` 同步。未开训、未采语言集。顺带核到 v3 **040000**。

## 14:04 SmolVLA 走哪条

用户问微调 smolvla_base 还是课表从零。结论：**W9 走官方微调**。课表 `--policy.type=smolvla --steps=20000` 是从零开关配微调步数；官方从零是 200K。本机百条 + 语言目标 + 8GB 都支持微调。现在不开。画布 `smolvla-route.canvas.tsx`。

## 13:54 Hub 开放数据适配

用户问官方 HF 数据集/权重能不能用。结论：LeRobotDataset **能加载**；`lerobot/svla_so101_pickplace` 关节名对齐，但相机是 `up`+`side`，校准域不同，**不能混进本机 ACT / 不能回放**。社区集多为小教程或双相机。开放权重 `lerobot/smolvla_base` 留 W9 微调本机数据。未下载、未开第二路训练。顺带核到 v3 **020000**。画布 `hub-dataset-adapt.canvas.tsx`。

## 13:56 刷新 v3 进度

用户要刷新训练进度。磁盘 **020000 @13:41:45**；进程仍在；GPU ~87%；估计 ~28K。20K loss 无日志。看板与台账已刷。

## 13:13 更新 v2 canvas

用户问 act-v2 canvas 为何没更新。原因：冻在 04:43 / 80K，循环 aborted 后没再写。已改成 v2 结案 + v3 在跑。watch-train 指向 v3。

## 13:11 开 ACT-v3

混合集 `so101_v3_mix` 111 ep / 98723。从零 bf16 100K。日志确认 `resume=False`，step 200 loss 6.994 / l1 0.674。约 9.5 step/s。

## 13:06 dagger3 两次 saved

用户报两次 saved。磁盘：**11 ep / 33623 帧 / 73 纠正**。日志最后是 `Episode saved (total: 10)` + `Final in-progress episode saved`。`Rollout finished`。401 Hub 不影响本地。采已够，先不训。

## 12:40 问 v3 是否和 v2 一样

答：采集操作几乎一样（E03 + Tab/Esc）。不同是新目录 dagger3；纠正要走完放置；训的时候混 100 条，不再只吃 DAgger。

## 12:35 按 v3 开采

用户确认按 v3 做，问怎么操作。课表「现在立刻」改为 E03 DAgger → `rollout_so101_dagger3`，15MB，Esc 收尾。先采不训。

## 12:34 问纠错为何没进 v2

澄清：61 段 Tab 纠正已在 dagger2 里，v2 整集都吃了（12942/37494）。没混的是原来 100 条专家示教。也没有只抽 `intervention=true`。当时设计是从零、只训 14 ep。

## 12:28 Position 0/15 + v3 方案

用户：Position 做满 15 次没有碰到或夹到；继续飞轮，先听方案。表改 Pos **0/15**。方案：E03 采 dagger3，混 100 专家训 v3；不混全量 dagger2；不用 v2 底策略。先不采不训。

## 12:26 v2 Position 找不到

用户：放在其他位置，完全找不到，空中空夹，动作很差。记 P016 定性失败，不编造次数。IID 0/15 + Pos 都差，日常回 E03。

## 12:08 窗口结束才松 → 0/15

用户确认放下是 60s 回原点，不是策略放置。第 1 轮从 1/15 改 **0/15**。

## 12:06 v2 IID 第 1 轮 1/15

用户：15 次里面结束的时候放下了；整臂前后抽；物体还在正下方。记 S×1 + Placement×14。若放下只是 60s 回原点，应改 0/15。

## 11:53 v2 夹后伸缩不放

用户：正下方夹取后不会放置，一直伸缩。终端：11:51 第一次 13s Ctrl-C；11:51:42 满 60s finished；11:53 又开一窗。归类 F-Placement / P019。未计满轮。E03 IID 仍更稳。

## 11:13 用户问训练结果

核磁盘未变：v2 `100000` @05:09，进程已停。答：训练跑完；只有 200–800 的 loss；20K 后无日志不编造；下一步按课表复测 W6 表。

## 11:12 ACT-v2 100K 齐

3 分钟监控循环再次 aborted。核磁盘：`100000` 于 **05:09:16** 落盘（约 197MB）；`last` 同步；`lerobot-train` 已停；GPU 空闲。100K loss 无日志，不编造。台账改成训练完成。下一步按课表：复测 W6 表，对比 ACT-v1 → v2。不重开监控循环，不并行 DP/VLA。

## 04:44 README 评测聚类 + 作者 + push

用户要求首页加入 `results/eval_w56.md` 聚类和作者信息并 push。README 新增 Results（按条件 / 失败模式聚类 + pie）和 Author（WANG, Jianlin / wjl110 / jianlinwang@ln.hk）。未改评测原数字。

## 04:13 启动 DSH

用户要启动 DeepSeek Harness。先 `npx @deepseek-ai/dsh web` 卡在 npmjs.org；改用 npmmirror 装全局 `@deepseek-ai/dsh@0.1.1-rc.2`，再 `dsh web`。已监听 http://127.0.0.1:3080。工作目录是用户主目录，不是仓库。ACT-v2 未停。Node 仍是 22.18.0（pi-ai 要 22.19+，仅警告）。

## 03:25 问 demo.mp4 用途

用户问当初为何要存 `assets/demo.mp4`。答：课表 W4 门槛产物（拔 Leader 后 30–60s 真机 Demo），不是 README GIF、也不是数据集里的 episode 视频。当时先落成 `demo_E03.mp4`，再改成课表固定名。文件约 105MB，超 GitHub 100MB，故未进库。

## 03:24 push

用户要求 push。提交 README 四格 Demo（`scraping.gif` 21.5MB、`corrective_demo.gif` 42.9MB）以及 W5–W7 台账/课表/评测。不带 `demo.mp4`（100.2MB 未压）、DAgger 数据、100 ep 补采视频、Captures 删除。

## 03:10 README 并排 GIF

用户给两张原片：`连续Scraping.gif`（1080×1920 / 31.5s / 247MB）、`corrective demonstration（纠正示教）.gif`（1080×1920 / 52s / 546MB）。按既有 Demo 规格压成 360×640 / 8fps / 128 色：`assets/scraping.gif` 21.5MB、`assets/corrective_demo.gif` 42.9MB。README Demo 第二行与 `demo.gif` / `demo_0.gif` 并排。原片已删，避免误进库。

## 01:29 采多少

用户问 DAgger 重采数量/时长。答：连续模式 50 是切集上限不是纠正次数；先保证落盘（至少 2 次 Episode saved），目标 30–50 次找物纠正，墙钟约 40–60 分钟。Esc 停，不 Ctrl-C，不训。

## 01:32 是不是纠正一小时

用户误解。澄清：40–60 分钟是整段墙上时钟（策略自己跑 + 间或纠正），不是 Tab 纠正满一小时。纠正大约 30–50 次，每次几十秒。

## 01:33 怎么知道 Episode saved

看 start.cmd 窗口日志 `Episode saved`，15MB 大约每 2 分钟一条。纠正时不会切集。Esc 后再等 `Final in-progress episode saved`。

## 02:07 不是训练

用户问训练情况。当前是 DAgger 采集：6 ep / 18966 帧已落盘，02:06 Episode 6 saved，进程仍在跑。没有 lerobot-train。先采不训。

## 04:22 监控 tick

估计 71K，ckpt 仍 60K，GPU 71%。循环继续。

## 04:19 监控壳 aborted

Cursor 停了 watcher/loop 两个壳。训练仍在（GPU 88%）。`watch-train.py` 14844 还在写 train-live。只重开 3 分钟循环。估计 68K。

## 04:10 监控 tick

估计 63K，ckpt 仍 60K，GPU 79%。循环继续。

## 04:07 监控 tick

**060000** 于 04:06:52 落盘。估计 60.5K，GPU 71%。下一个 80K。循环继续。

## 04:04 监控 tick

估计 61K，磁盘仍无 060000。进程 56620 仍高 CPU，GPU 53%。先当写盘延迟，不是 P014。循环继续。

## 04:01 监控 tick

估计 59K，ckpt 仍 40K，GPU 77%。60K 将至。循环继续。

## 03:58 监控 tick

估计 57K，ckpt 仍 40K，GPU 67%。60K 将至。循环继续。

## 03:55 监控 tick

估计 55K，ckpt 仍 40K，GPU 72%。接近 60K。循环继续。

## 03:52 监控 tick

估计 53K，ckpt 仍 40K，GPU 85%。循环继续。

## 03:49 监控 tick

估计 51K，ckpt 仍 40K，GPU 89%。循环继续。

## 03:46 监控 tick

仍正常：估计 49K，ckpt 仍 40K，GPU 83%。循环继续。

## 03:51 还是不懂总 loss 和 KL

用户没听懂 VAE KL。改用：l1=手臂准不准；KL=小纸条正不正常；总 loss=两件事一起改网络。推理时只抽正态 z，所以训练必须压 KL。

## 03:44 loss 和 l1_loss

用户问区别。答：l1_loss 是动作预测的平均绝对误差；loss = l1_loss + 10×kld_loss（本机 ACT kl_weight=10）。盯真机更看 l1。

## 03:42 开实时监控

用户要实时训练进度。已开 `scripts/watch-train.py`（2s）→ `lab-log/train-live.html`，并写 canvas。顺带核到 **040000**（03:33:13）。估计约 46K，GPU ~60%，正常。循环 3 分钟刷新画布。

## 03:28 训练正不正常

用户问进度。进程在，GPU 85% / 4.6GB，5 秒 CPU +5.1，不是 P014 那种冻住。ckpt 仍是 020000。按速度估计约 37K，正常。

## 03:22 监控 aborted

Cursor 任务 `Start ACT-v2` 显示 aborted。核盘：`020000` 已齐；`python` 56620 / `lerobot-train` 仍在。结论：断的是监控，不是训练。日志停在约 25643。

## 03:04 纠正叫什么名词

用户问 Tab 示范纠正的专有名词。答：LeRobot 字段是 intervention；方法是 DAgger / HITL；那段人手动作是 corrective demonstration / expert correction。

## 02:58 纠正数据这波训了吗

用户问示范纠正有没有进 ACT-v2。答：进了。全量 37494 帧都在训，其中 Tab 纠正 12942 帧（34.5%）。没有按 intervention 过滤。第一趟 P018 的纠正没落盘，不在这波。

## 02:54 是不是只训 14 条、没混 100

用户确认设计。答：对。v2 `resume=False`、`pretrained_path=None`，只读 dagger2 14 ep / 37494 帧，未混原 100。自主帧 65.5%。风险成立（覆盖、学失败），但不是把 E03 权重忘掉——E03 仍在盘上。14 条是约 2 分钟连续切集，不是 14 条 20 秒示教。

## 02:33 开 ACT-v2

用户说开始下一步。按课表开 `act_so101_v2`（dagger2 根 + bf16）。02:33:15 step 200 loss 6.967，约 11.4 step/s。未并行 DP/VLA。

## 02:24 采集能不能用

用户问采集数据是否可用。进程已于 02:22:30 `Rollout finished`。磁盘 **14 ep / 37494 帧**，纠正 **61 段 / 34.5%**，视频 `file-000`～`012` 齐。结论：落盘可用、纠正量够；课表旧 ACT-v2 命令仍指向 100 ep，不要直接贴；单独训 dagger2 会带上约 65% 策略自跑帧。未开训。
