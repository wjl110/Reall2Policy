# 会话 2026-09-15

## 09:45 push GitHub

用户要推远程。不推 `assets/demo.mp4`（100.2MB）和 100 ep 的 `file-003/004.mp4`（约 132MB），以及 dagger/v3_mix（P009）。只推台账、课表、评测与脚本。下一步仍是 EMA 真机。

## 09:43 倒计时 watcher 任务中止

Cursor 后台倒计时任务 aborted。训练早已结束（100K @08:34），GPU 空闲。已停掉残留的 `watch-train.py`。下一步仍是 EMA 真机对照。

## 08:34 DP 100K

磁盘 **100000 @08:34:20**，含 `pretrained_model_ema`。墙钟约 36.7 h。80K @01:27。100K loss 无日志。下一步 EMA IID。日常仍 E03。

## 00:05 问 DP loss 正不正常、最终要到多少

用户问 loss 等参数是否符合预期、多少才算正常。答：DP 的 loss 是 **噪声 ε 的 MSE**，和 ACT 的 0.168（L1+KLD）不能比。已有日志 0.564@200 → 0.021@10–14K，先陡降再平台，形态正常。课表没有 loss 及格线；W8 正常标准是 **100K 落盘 + EMA 真机对照 E03**。80K 尚未出现（00:05，窗口约 00:06）。不要停训。
