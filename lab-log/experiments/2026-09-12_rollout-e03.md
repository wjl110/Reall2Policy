# 实验：E03 真机 rollout

- 状态：完成（连续抓取 OK；`assets/demo.mp4` 已齐）
- 时间：2026-09-12 约 12:30
- 操作者：用户
- 课表：W4

## 做了什么

```bat
lerobot-rollout --strategy.type=base --policy.path=outputs/train/act_so101_e03/checkpoints/100000/pretrained_model --robot.type=so101_follower --robot.port=COM4 --robot.id=so101_follower --robot.cameras="{ front: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" --task="Pick up the object and place it down." --duration=60 --display_data=true
```

## 实验数据

| 项 | 值 |
|----|----|
| 权重 | `act_so101_e03/checkpoints/100000` |
| 时长 | 60s（课表默认） |

## 效果

用户报告：连续抓取没问题；没有出现折叠态动作。P013 在这次未再现。单次未计次，不算 W5–6 评测。

12:39 落盘 `assets/demo_E03.mp4`（约 105MB）。课表要的是 `assets/demo.mp4`，不是 GIF。README 现有 `demo.gif` / `demo_0.gif` 是 09-10 遥操展示。

## 结论

12:47 用户已把文件落到课表名 `assets/demo.mp4`（约 105MB）。W4 完成。进库前需压缩。下一步 W5–6 IID 计次，不要跳去 DP/VLA。
