# 方案：W7 飞轮 v3（未执行）

- 状态：已确认，先采 dagger3
- 时间：2026-09-13 12:28
- 课表：W7

## 为什么 v2 失败

只从零训 dagger2 14 ep。65.5% 是策略自跑（含失败空夹）。纠正偏找物，干净抓放少。结果：IID 0/15（P019），Position 0/15（P016）。

## 推荐（一条线）

1. **日常权重保持 E03。** 不用 v2 去采、不去训。
2. **新目录 DAgger**：`rollout_so101_dagger3`。策略必须 E03。不 resume 100 ep，不 resume dagger2。
3. **采什么**：物体离开夹下；一露出空夹/找错点就 Tab；纠正要走完「找到 → 夹起 → 放到桌上松开」。夹下若泵臂也纠放置。Esc 停，等 `Episode saved`（P018）。
4. **训 ACT-v3 新目录** `outputs/train/act_so101_v3`：从零，**100 专家 + 新纠正**。不要全量 dagger2（失败自跑会再污染）。不要 resume v2。本机 bf16、100K、batch 8。
5. **复测**：先夹下 15，再 Position 15。对照 E03 / v2。

## 不选

- 只用 dagger 再从零训（v2 已证伪）。
- 用 v2 当 DAgger 底策略。
- 现在并行 DP / SmolVLA。
- 把 dagger2 整段（含空夹）混进 v3。

## 进度

2026-09-13 12:35 用户确认按 v3 做。先采 `dagger3`，采齐再混 100 ep 训。
2026-09-13 13:11 混合集 111 ep 已齐，ACT-v3 已开训。
