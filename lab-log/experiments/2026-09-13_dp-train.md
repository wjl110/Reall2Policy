# 实验：W8 Diffusion（同一 100 ep）

- 状态：成功
- 时间：2026-09-13 19:50 – 2026-09-15 08:34
- 课表：W8 / E07

## 做了什么

用户确认今天开 W8。按课表用 E03 同一数据集训 Diffusion，本机加 bf16（8GB）。

```bat
lerobot-train --dataset.repo_id=local/so101_pick_place --dataset.root=D:\SO-ARM101\data\local\so101_pick_place_20260910_011033 --dataset.video_backend=pyav --policy.type=diffusion --ema.enable=true --output_dir=outputs/train/dp_so101_v1 --job_name=dp_so101_v1 --policy.device=cuda --wandb.enable=false --policy.push_to_hub=false --batch_size=8 --steps=100000 --accelerator.mixed_precision=bf16
```

## 实验数据

| 项 | 值 |
|----|----|
| 数据 | `so101_pick_place_20260910_011033` **100 ep / 65100 帧** |
| 输出 | `outputs/train/dp_so101_v1` |
| 对照 ACT | E03 `act_so101_e03/100000` · IID 37/90 · Pos 0/90 |
| 设计 | 从零，不混 dagger / v3_mix；EMA 开 |

## 效果

- 19:48 第一次失败：缺 `diffusers`（P021）。已 `pip install -e .\lerobot[diffusion]`。
- 19:50 重开成功。`resume=False`。100 ep / 65100 帧。263M。EMA 开。
- `step:200` loss **0.564**；`400` 0.112；`600` 0.083；`800` 0.053；`1K` **0.047** / grdn 0.902 / mem 6.14 GB。
- 速度先约 4.9 step/s，约 780 掉到 ~2.4，**1215 再掉到 1.73 s/step**（tqdm ~48 h）。
- 20:05 ~1526 步，**2.4 s/step**，ETA ~65 h。`updt_s` 1.60（早先 0.17–0.36），`data_s` 仍 0.028。GPU 100% / 40W / 54°C。没卡死。
- 20:21 ~2080 步，loss 0.032，约 1.3 s/step。大屏全屏被 `meta refresh` 顶掉（P022），已改成本机 8765 静态页。
- 2026-09-14 ckpt：**020000** 03:13、**040000** 10:31、**060000** 17:18。间隔约 6.8–7.3 h。23:57 进程仍在，GPU ~93% / 51 W。终端 capture 停在 14K，大屏 step 跟 ckpt。INFO loss 到 **0.021 @14K**。未到 80/100K。
- 2026-09-15 00:05 用户问 loss 是否正常。结论：DP loss=预测噪声的 MSE（`prediction_type=epsilon`），与 ACT 0.168 不可比。INFO 到 14K 已平台在 ~0.021，属常见形态。课表不以 loss 判及格，以 100K + 真机对照为准。
- **080000 @01:27:45**、**100000 @08:34:20** 落盘。墙钟约 36.7 h。进程已退出。100K loss 无日志，不编造。EMA 目录存在。
- 下一步：`pretrained_model_ema` 真机 IID，填 `results/act_vs_dp.md`。不要开 VLA。
