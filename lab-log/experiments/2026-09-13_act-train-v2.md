# 实验：W7 ACT-v2（dagger2）

- 状态：进行中
- 时间：2026-09-13 02:32
- 课表：W7 / E06

## 做了什么

DAgger 采集已齐，开 ACT-v2。数据根是 `rollout_so101_dagger2`，不是旧 100 ep。本机 8GB 加 `--accelerator.mixed_precision=bf16`（与 E03 相同）。

```bat
lerobot-train --dataset.repo_id=local/rollout_so101_dagger2 --dataset.root=D:\SO-ARM101\data\local\rollout_so101_dagger2 --dataset.video_backend=pyav --policy.type=act --output_dir=outputs/train/act_so101_v2 --job_name=act_so101_v2 --policy.device=cuda --wandb.enable=false --policy.push_to_hub=false --batch_size=8 --steps=100000 --accelerator.mixed_precision=bf16
```

## 实验数据

| 项 | 值 |
|----|----|
| 数据 | `rollout_so101_dagger2` 14 ep / 37494 帧 |
| 输出 | `outputs/train/act_so101_v2` |
| 目标 | 100000 steps，batch 8，bf16 |
| 权重 | 新目录，不 resume E03 |

## 设计（2026-09-13 02:54 核对）

- 从零训：`resume=False`，`pretrained_path=None`，不加载 E03。
- 只读 dagger2 14 ep / 37494 帧，**未混**原 100 专家示教。
- 纠正 34.5%（12942），自主 65.5%（24552）。自主帧含成功和失败。
- 已知风险：找物覆盖偏这次会话；BC 会学策略失败帧；v2 可能弱于 E03 的夹下 IID。E03 权重仍保留。

## 效果

- 02:32:49 读到 14 ep / 37494 帧。
- 02:33:15 `step:200` loss **6.967** / l1 0.705 / kld 0.626 / grdn 156.5；约 **11.4 step/s**；显存 2.05 GB。
- 早期：400 / 3.112；600 / 2.688；800 / 2.413。
- 03:03:41 **`checkpoints/020000` 已落盘**（含 `model.safetensors`）。20K 的 loss 日志被进度条覆盖，不编造。
- 03:22 Cursor 监控 aborted；`lerobot-train` / `python` 仍在。日志最后见到约 step 25643。
- 03:28 体检：GPU 85% / 4602 MiB，5 秒 CPU +5.1，**不是 P014 冻结**。按 11.4 step/s 估计约 37K。
- 03:33:13 **`checkpoints/040000` 落盘**（model 207MB）。
- 03:42 开 `scripts/watch-train.py` → `lab-log/train-live.html`。估计约 46K，GPU ~60%。
- 04:06:52 **`checkpoints/060000` 落盘**。60K loss 无日志。
- 04:40:08 **`checkpoints/080000` 落盘**。80K loss 无日志。
- 训练占用 GPU 时不要再录数据、不要 resume dagger2。

## 节点

- GPU：RTX 5060 Ti 8GB
- 关联：P006 pyav、P005 last 软链、P014 进度条冻结
