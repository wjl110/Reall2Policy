# 当前状态

更新：2026-09-13 03:22

## 课表

**W7 进行中。** ACT-v2 **进程仍在跑**。Cursor 监控已断（task aborted），不是训练自己报错停。不要并行 DP/VLA。不要录数据、不要 resume dagger2。

## 正在跑

`lerobot-train` / `python` 02:32 起仍在。日志捕获停在约 **step 25643**。磁盘已有 **`checkpoints/020000`**（03:03:41）。

## 最新可用数据

| 项 | 值 |
|----|-----|
| 原数据 | 100 ep / 65100 帧 |
| DAgger | `rollout_so101_dagger2` 14 ep / 37494 帧 |
| 权重 | E03 `100000`；v2 **`020000`** |

## 阻塞 / 未完成

- 等 40K / 100K。监控窗口不必再看。
- 若进程真死了，从 `020000` resume，不要重开空目录。

## 下一步（按课表）

等 ACT-v2 到 100K。不要并行 DP/VLA。
