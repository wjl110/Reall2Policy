# Week 9–10。官方微调，不要从零。batch 先 4，OOM 再降。
policy_path: lerobot/smolvla_base
device: cuda
batch_size: 4
steps: 20000
video_backend: pyav
push_to_hub: false
wandb: false
dataset_repo_id: local/so101_lang
dataset_root: D:\SO-ARM101\data\local\so101_lang
tasks:
  - "Pick up the red block."
  - "Put the block into the box."
  - "Move the blue object to the left."
